---
name: DefaultLockup
menu: Core
---

import { Playground, Props } from 'docz'
import DefaultLockup from './DefaultLockup'

# DefaultLockup

> The default logo and text lockup treatment for the Spruce brand.

<Playground>
	<div
		style={{
			display: 'inline-block',
			backgroundColor: '#2c323d',
			padding: '8px'
		}}
	>
		<DefaultLockup />
	</div>
</Playground>

## When to use

DefaultLockup should be used in the PrimaryHeader when the user isn’t interacting with an organization or location.

## When not to use

Don’t use the DefaultLockup in situations where the user is interacting with an organization or location.

## Props

This component has no props.

<Props of={DefaultLockup} />
