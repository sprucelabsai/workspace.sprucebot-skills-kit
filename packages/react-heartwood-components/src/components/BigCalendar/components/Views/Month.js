// @flow
import React, { Component } from 'react'
import cx from 'classnames'

type Props = {
	text?: string,
	children?: Node,
	className?: string
}

const Month = (props: Props) => {
	return <div>I'm the month view</div>
}

export default Month
