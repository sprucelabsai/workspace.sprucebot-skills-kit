// @flow
import React, { Component } from 'react'
import cx from 'classnames'

type Props = {
	text?: string,
	children?: Node,
	className?: string
}

const WeekRow = (props: Props) => {
	return <div>I'm the week row view</div>
}

export default WeekRow
