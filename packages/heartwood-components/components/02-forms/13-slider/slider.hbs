<div class="{{ Classes.SliderWrapper }}s">
	{{> @input-pre }}
	<input
		type="range"
		class="{{ Classes.Slider }}"
		id="{{ id }}"
		name="{{ name }}"
		min="{{ min }}"
		max="{{ max }}"
		value="{{ value }}"
		step="{{ step }}"
		style="--min: 0; --max: 100; --val: 50;"
	>
</div>