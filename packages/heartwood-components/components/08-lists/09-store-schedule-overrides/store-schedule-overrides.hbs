<div class="list-wrapper">
	{{> @list-header }}
	{{> @bottext text="Modify the store schedule for holiday closings and special hours. The store schedule will override teammate schedules."}}
	{{> @list }}
	{{> @button className="btn-secondary btn-full-width" text="Add a Store Schedule Override"}}
</div>
